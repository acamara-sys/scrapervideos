## Getting Started

First, run the development server:

```bash
npm install
npm run dev
# then 
npx playwright install  
```

## Rajoute les variables d'environnement

```
mkdir .env 
# then add the api youtube data key
API_KEY=api key
```